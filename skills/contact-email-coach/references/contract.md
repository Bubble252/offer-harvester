# Contact Email Coach Contract / 套磁信教练契约

The adapter accepts an existing `target_id` and optional `mode`. Supported modes are `new`, `rewrite`, `advisor_alignment`, `reduce_exaggeration`, and `follow_up`.

The result contains a candidate draft, reviewer output, evidence audit, quality report, evidence refs, and risk tags. It never sends email or updates application status.
