# Advisor Due Diligence Contract / 导师尽调契约

The adapter accepts an existing `advisor_id` and optional `target_id`. It returns a candidate report that separates verified source coverage, profile gaps, review questions, and risk signals.

Community content is not a fact source. It must be labelled as a risk signal with provenance and requires human review.
