# 安全与隐私

[English](SECURITY.md)

Offer Harvester 处理的内容可能包含学生申请隐私。请遵守以下规则：

- 不提交真实简历、成绩单、推荐信、证书、联系方式或邮箱导出。
- 不提交 `.env`、API key、token、私钥、云数据库连接串或模型凭据。
- 不把真实导师网页全文、私密社区内容或登录后内容复制到公开仓库。
- 不把生成材料当作已经核验的事实；用户必须在发送或提交前自行复核。
- 应用默认不会自动发送套磁邮件、提交申请或将候选结果写成 confirmed fact。
- 邮箱信号第一版是只读导入；候选信号需用户确认后才能影响 tracker。
- 远程 DSH 调用应使用单独的 scoped plugin token，不复用 LLM provider key。

## 报告问题

不要在公开 issue 中发布密钥、真实资料、私有 URL 或完整邮件正文。安全问题请通过项目维护者配置的私密渠道报告，并提供最小可复现信息。
